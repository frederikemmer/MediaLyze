Desktop builds bundle `ffprobe` binaries at packaging time.

Use LGPL-compatible FFmpeg/ffprobe distributions and place their license texts in this directory
alongside the bundled binaries for each release artifact.
