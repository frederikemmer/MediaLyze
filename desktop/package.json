{
  "name": "medialyze-desktop",
  "version": "0.10.0",
  "private": true,
  "description": "Desktop packaging for the MediaLyze local media analysis app.",
  "author": "Frederik Emmer",
  "main": "main.cjs",
  "scripts": {
    "dev": "electron .",
    "test": "node --test ./ffprobe-paths.test.cjs ./package.test.cjs ./scripts/build-backend.test.mjs",
    "generate:icons": "node ./scripts/generate-icons.mjs",
    "dist": "npm run generate:icons && electron-builder",
    "dist:dir": "npm run generate:icons && electron-builder --dir",
    "build:backend": "node ./scripts/build-backend.mjs"
  },
  "devDependencies": {
    "electron": "^39.8.5",
    "electron-builder": "^26.0.12",
    "icon-gen": "^5.0.0"
  },
  "build": {
    "appId": "io.github.frederikemmer.medialyze",
    "productName": "MediaLyze",
    "directories": {
      "output": "../dist/desktop-app",
      "buildResources": ".generated-icons"
    },
    "files": [
      "main.cjs",
      "preload.cjs",
      "ffprobe-paths.cjs"
    ],
    "extraResources": [
      {
        "from": "../frontend/dist",
        "to": "frontend-dist"
      },
      {
        "from": "../dist/desktop-backend",
        "to": "backend"
      },
      {
        "from": "resources",
        "to": "resources"
      }
    ],
    "mac": {
      "target": [
        "dmg"
      ],
      "artifactName": "${productName}-arm64.${ext}",
      "icon": "medialyze.icns",
      "category": "public.app-category.video"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "artifactName": "${productName}.${ext}",
      "icon": "icon512.png",
      "category": "AudioVideo"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "artifactName": "${productName}.Setup.${ext}",
      "icon": "medialyze.ico"
    }
  }
}
